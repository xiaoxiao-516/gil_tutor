import { TaskManagementPage } from "./TaskManagementPage";

/** 与「布置」页相同的信息结构与交互，仅视觉为 `appearance="studio"` 变体 */
export function TaskManagementPageStudio() {
  return <TaskManagementPage appearance="studio" />;
}
